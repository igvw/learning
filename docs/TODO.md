# TODO

This file tracks the short near-term list. The broader backlog lives in [roadmap.md](roadmap.md).

- Define QML v2 around bundles and retire math-style `computed_text` as the long-term primary authoring path.
- Redesign the stats-page `Add` flow so regular users can create questions smoothly without seeing QML.
- Align the question editor, import flow, and docs with the split between admin-only QML tooling and structured regular-user authoring.
- Keep the remaining contributor/moderation UX polish focused on real friction, not broad workflow redesign.
