# General

```text
Generate only final QML.
Do not output markdown, headings, bullets, comments, JSON, filenames, or explanations.
Use one question or bundle block per entry.
Use | for alternatives in one slot.
Use , for separate required slots.
Use trailing {...} for unordered multi.
Use trailing [...] for single or ordered.
Use embedded [...] inside the sentence for inline cloze.
Use top-level { ... } blocks for bundles.
Write natural learner-facing text.
```
