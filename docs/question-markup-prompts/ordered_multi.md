# ordered_multi

```text
Write only ordered_multi QML.

Shape:
Prompt [slot1a | slot1b, slot2a | slot2b]

Rules:
- use one comma-separated slot per required answer
- use | for alternatives inside one slot
- slot order must matter

Example:
Prompt text [first answer, second answer]
```
